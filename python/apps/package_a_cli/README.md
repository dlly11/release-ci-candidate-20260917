# Example Package A CLI

Command-line application that exposes the greeting service from `release-candidate-package-a`.

```python
from release_candidate_package_a_cli import main

main(["Ada"])  # Prints: Hello, Ada!
```

See the [component documentation](https://dlly11.github.io/release-ci-candidate-20260917/python/apps/package_a_cli/docs/index.html) for examples and API details.
